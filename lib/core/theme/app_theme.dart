import 'package:flutter/material.dart';

import 'app_colors.dart';

class AppTheme {
  static ThemeData get theme => ThemeData(
      fontFamily: 'Montserrat', primarySwatch: AppColors.primaryColor);
}
